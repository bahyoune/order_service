spring:
  application:
    name: order-service

  cloud:
    config:
      uri: http://172.17.0.1:8888
      fail-fast: true
      retry:
        max-attempts: 6
        initial-interval: 1000
        max-interval: 5000
        multiplier: 1.5
